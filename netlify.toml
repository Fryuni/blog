[build]
  publish = "dist"
  command = "npm cache clean --force && yarn cache clean && yarn build"
[build.processing.js]
  bundle = false
  minify = true
[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"
